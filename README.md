# Prism docs

MDX site, versioning, Algolia search, and generated CLI reference.

## Structure

- `src/content` — versioned guides + install docs.
- `src/components` — MDX shortcodes, version picker.
- Search index built in CI via `pnpm algolia:reindex`.

## Dev

```bash
pnpm install
pnpm dev
```

---

*Fictional stub for Diffkit local dev — not a real product repo.*
